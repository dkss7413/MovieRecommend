<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/homeMovieView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <ImageView
        android:id="@+id/blackCircle"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:alpha="0.4"
        app:layout_constraintLeft_toLeftOf="@id/homeMovieView"
        app:layout_constraintTop_toTopOf="@id/homeMovieView"
        app:srcCompat="@drawable/black_circle" />

    <TextView
        android:id="@+id/rankingNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/blackCircle"
        app:layout_constraintLeft_toLeftOf="@id/blackCircle"
        app:layout_constraintBottom_toBottomOf="@id/blackCircle"
        app:layout_constraintRight_toRightOf="@id/blackCircle"
        android:text="1"
        android:textColor="#FFFFFF"/>
</androidx.constraintlayout.widget.ConstraintLayout>